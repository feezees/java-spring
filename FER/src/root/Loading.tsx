import { Header } from "../ui/Header"
import { Layout } from "../ui/Layout"

export const Loading = () => {
    return (
        <Layout>
            <Header>
                <p>Loading...</p>
            </Header>
        </Layout>
    )
}